# P19 探针报告 —— 端到端运行器(最终测试入口)

状态:**✅ 无文件合成演示全链路通过**(0 error、门禁通过、项目包校验 OK)

**更新(第19轮):真实模式已接入正式提示词**——翻译用 `prompts/translation.json` 原文(系统提示词 + 你的完整翻译要求),AI 复审用 `prompts/audit.json` 原文;因 file:// 页面无法 fetch 本地 JSON,提示词以内嵌常量随代码零改动同步。

**更新(第24轮):新增场景 B**——同时选择英文 PDF + 中文 PDF(如你已有的 ZK-Tracer 中文翻译版)时,跳过翻译,走"锚点 + 块级对齐 → 审核 → 复审 → 门禁 → 项目包";对齐引擎输出的 1:0 块按 warn 待人工确认,不阻塞门禁。合成场景 B 实测:errors=0,warns=1,门禁通过,包校验 OK。

**更新(第30轮):翻译断点持久化**——每个块成功后写入 localStorage(按文件名的独立会话),刷新/关闭后重跑自动续跑;新增"清除翻译会话"按钮。合成演示连跑两遍实测:第二遍 `已存 9 块 / 完成 9(复用 9)/ 失败 0`,证明不重跑已完成块。

## 探针目标

为 ZK-Tracer 真实论文端到端测试提供浏览器运行器:
- 阶段0:选择 PDF → pdf.js(CDN 懒加载)解析 → 内联 P4/P5 算法切块
- 阶段1:翻译(mock 离线 / DeepSeek 真实切换,API Key 仅存本机 localStorage)
- 阶段2:双栏分页排版
- 阶段3:场景 A 块级对齐(管线直出)
- 阶段4:规则审核(8 条规则)
- 阶段5:AI 复审(mock)
- 阶段6:人工门禁 + 项目包 JSON(浏览器下载,含 checksum)

## Debug 记录(本轮发现并修复 2 个 mock 质量问题)

1. mock 章节翻译成"第1章 Introduction",章节编号正则识别不到 → R2 误报 error。改为"1 节"形式。
2. mock 摘要未带 zkVM 缩写 → R6 warn。补上首次出现格式。
(这恰好证明 R2/R6 规则在真实翻译中能抓住同类问题。)

## 浏览器实测(P19 合成演示)

```
阶段1 翻译:9/9 完成,术语 3
阶段2 分页:页数 1,块序 OK
阶段4 规则审核:errors=0 warns=1(R8:数字 18.3 缺失,不阻塞)
阶段5 AI 复审:通过
阶段6 门禁:通过;项目包校验 OK(checksum=4541c078)
✅ 合成演示完成
```

## 真实论文操作步骤(需要你执行)

1. 浏览器(联网)打开 `probes/P19-e2e-runner.html`
2. 选"无文件合成演示"确认链路(可选)
3. 选择 `Desktop\文献\导师文章\18：ZK-Tracer：...pdf`
4. 翻译引擎选 mock → 先跑通全流程(不花 API)
5. 再选 real,填 DeepSeek Key,跑真实翻译(你的提示词已存 `prompts/translation.json`,当前 runner 用简化提示词,正式接入时替换)
6. 下载项目包 JSON,把"阶段输出"内容发我

这就是"用我们那篇已有论文进行测试"的最终入口。
