# P18 探针报告 —— 全链路集成(Sprint 0–6 模块串行)

状态:**✅ 浏览器实测 ALL-PASS**(六个共享核心一次串通)

## 探针目标

在"解析已完成"的前提下,把全部已 debug 模块串成端到端流水线:
翻译(两遍法+断点) → 中文分页(双栏) → 块级+句级+词级对齐 → 规则审核 → AI 复审 → 人工门禁 → 项目包校验和。

## Debug 记录(本轮发现并修复 2 个集成口径问题)

1. **把 figure 块送进了翻译管线**(真实流程图/表/公式不过模型)→ 翻译只输入文字块,非文字块原样并入中文文档。
2. 页数断言过严(短夹具双栏 1 页合理)→ 调整为 >=1。

## 浏览器实测(ext-p18-integration-test.html)

```
ALL-PASS
✅ 阶段1 翻译:9/9 完成,术语>=3
✅ 阶段2 分页:块序保持
✅ 阶段3 对齐:句级2句 + span 合法
✅ 阶段4 规则审核:0 error
✅ 阶段5 AI 复审:通过
✅ 阶段6 门禁+项目包:校验通过
```

## Vitest 落地

`tests/integration/e2e-synthetic.spec.ts` —— 与浏览器探针同流程同夹具。

## 真实 ZK-Tracer 端到端的替换点(已全部预留注入接口)

| 环节 | 合成探针 | 真实论文 |
|---|---|---|
| 解析 | 手工 blocks | P4/P5/P6/P7 解析器(需 npm 环境跑 TS 或浏览器接 pdf.js) |
| 翻译 | mockTranslate | DeepSeek client + prompts/translation.json |
| 分页/对齐/审核/打包 | 同一份共享核心 | 同一份共享核心 |

下一步:你本机跑 `check.ps1`(14 个测试文件),然后我们接真实 DeepSeek 跑 ZK-Tracer。
