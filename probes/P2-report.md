# P2 探针报告 —— 双栏中文打印导出文字层

状态:**预览布局 ✅ 已验证;打印 PDF 文字层 ⏳ 待 P4 联测(需你执行一次打印导出)**

## 探针目标

验证 Chrome "打印 → 另存为 PDF" 产物:
1. 中文文字是否保留为可选中文字层(而非栅格图);
2. 双栏内容被 PDF.js 重新解析后,阅读顺序是否与导出前一致。

## 已完成

- 确定性固定数据(固定种子内容),保证两次解析可比;
- 页面按自研分页算法绝对定位排版:通栏标题区 + 双栏正文 + 跨栏表 + 图/公式块;
- 打印样式:@page A4、边距 0、每页 `page-break-after`;
- 页面内提供"自检基准 JSON"(导出前每个块的 页/栏/y/高/文本头),复制后供 P4 比对;
- 预览布局已通过 Chrome 截图回归:页边距虚线、双栏栏线、图/表/公式块位置均正常。

## 待执行验证(需你操作)

1. 打开 `probes/P2-print-text-layer.html`
2. 点"打印 / 另存为 PDF" → 目标"另存为 PDF" → **边距选"无"、关闭页眉页脚**
3. 保存到任意位置
4. 打开 `probes/P4-pdf-text-layer.html`(需联网,CDN 加载 pdfjs)→ 解析该 PDF
5. 复制 P2 的"自检基准 JSON"粘到 P4 比对框 → 点"比对"

通过标准:P4 显示"✅ 比对通过"。失败则 P4 导出的阅读顺序 JSON 会自动暴露错位位置。
